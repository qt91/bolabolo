//Remove all gameobject in child
public void ClearObjChild()
{
    int childs = transform.childCount;
    for (int i = childs - 1; i > 0; i--)
    {
        Destroy(transform.GetChild(i).gameObject);
    }
}